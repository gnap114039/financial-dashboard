# 退休資產追蹤 — Spec v0.2

## 1. 專案目標

個人退休財務追蹤工具，用於每月監控資產增長，並預測退休計劃是否在軌道上。僅供個人使用。

---

## 2. 使用者

- 單一使用者（本人），不需要登入或權限控制

---

## 3. 核心需求

### 3.1 資產追蹤

追蹤以下兩種資產類型：

| 類型 | 說明 |
|---|---|
| 銀行現金存款 | 各銀行帳戶的現金餘額（多幣別） |
| 股票 / ETF | 個股與 ETF 持倉，含券商現金部位 |

### 3.2 投資策略背景（供計算參考）

- 每月固定投入約 **8 萬 TWD**
- IB01 作為現金停泊，等待好的進場價格再換成個股或其他 ETF
- 個股與 ETF 以回測後的好價格買入為主，非固定定投

### 3.3 退休目標

| 參數 | 數值 |
|---|---|
| 目前年齡 | 33 歲 |
| 目標退休年齡 | 45 歲（距今 12 年） |
| 退休目標資產（2026 購買力） | 3,000 萬 TWD |
| 退休目標月支出（2026 購買力） | 10 萬 TWD／月 |
| 通膨假設 | 3.2%／年 |

> 所有目標金額以 2026 年購買力為基準，系統需根據通膨率自動換算至退休當年（45 歲）的實際目標金額。

### 3.4 數據輸入方式（已決策）

**採用 Google Sheets 公開 CSV 方式**，手動更新 Google Sheets 後，網頁重新整理自動取得最新資料。

| 資料 | Google Sheets 工作表 |
|---|---|
| 股票 / ETF | `stock` 工作表，欄位：Ticker, Total Share, Average, Total Price, Current Price |
| 銀行現金 | `bank_cash` 工作表，欄位：Bank, NTD, USD, EUR, JPY, MYR, CNY |

**`Current Price` 欄位說明：** 供無法透過 Finnhub API 取得報價的持倉手動填入（如 LSE 掛牌的 IB01），其餘填 `-` 由 API 自動取得。

**`PRINCIPAL` 特殊列：** `Ticker` 填 `PRINCIPAL`，`Total Price` 填入歷史累積實際投入本金（USD）。前端自動讀取並顯示，不計入持倉計算。

### 3.5 資料快照機制

本機開發環境（localhost）透過 FastAPI 後端每日自動存一份快照：

- `src/data/stock/stock_YYYY-MM-DD.csv`
- `src/data/bank/bank_YYYY-MM-DD.csv`

同一天多次重新整理只覆蓋當日快照，不重複下載。快照資料已加入 `.gitignore`，不納入 Git 版本控制（僅存於本機）。

前端渲染時，透過 Finnhub 取得的即時股價會自動回寫今日快照的 `Current Price` 欄位（僅更新值為 `-` 的列，不覆蓋手動填寫的價格）。同日後續刷新直接從快照讀取，無需重複呼叫 Finnhub API。

---

## 4. 頁面功能需求

### 4.1 總覽區塊 ✓
- 目前總資產（支援多幣別切換顯示）
- 距離退休目標的進度（百分比）
- 45 歲退休所需實際金額（通膨換算後）
- 預估可退休年齡（月精度）
- 實際投入本金
- 總投資收益

### 4.2 資產配置區塊 ✓
- 銀行現金：三層 Accordion（總覽 → 各銀行 → 幣別明細）
- 股票 / ETF：Accordion 展開後可切換四種視圖
  - **清單**：各持倉明細（持倉中 / 券商現金 / 已清倉）
  - **配置**：比例色條，hover 高光對應項目
  - **比較**：橫向 bar chart，依市值排序，顏色反映損益
  - **圓餅**：Donut chart（Chart.js），含各持倉比例與損益

### 4.3 投資報酬明細 ✓
- 實際投入本金（USD 手動輸入）
- 利息 / 股息收益、資本利得、總投資收益

### 4.4 退休預測明細 ✓
- 可編輯：退休目標、每月投入、通膨假設、預估年化報酬率

### 4.5 每月花費統計（待開發）
- 每月花費趨勢
- 類別分析

### 4.6 成長趨勢（待開發）
- 每月資產總額歷史走勢圖
- 實際值 vs 目標軌跡線

---

## 5. 設計規範

| 項目 | 規格 |
|---|---|
| 風格 | 暗色系 Dashboard |
| 使用裝置 | 桌機為主（個人使用） |
| 語言 | 繁體中文 |
| 前端技術 | 靜態 HTML + CSS + JavaScript |
| 後端技術 | FastAPI（Python）+ Docker |
| 幣別切換 | 動態偵測 CSV 幣別欄位，使用 open.er-api.com 匯率 |
| 部署 | GitHub Actions → GitHub Pages，密碼保護（SHA-256 hash） |
| 圖表 | Chart.js（CDN），用於股票圓餅視圖 |

---

## 6. 待決策項目

| 項目 | 狀態 |
|---|---|
| 每月花費統計頁面設計 | 討論中 |
| 成長趨勢圖（利用快照資料） | 待開發 |

---

## 7. 版本紀錄

| 版本 | 日期 | 說明 |
|---|---|---|
| v0.1 | 2026-05-09 | 初版需求整理 |
| v0.2 | 2026-05-24 | 更新架構：Google Sheets 輸入、FastAPI 快照後端、多幣別切換、已完成功能標記 |
| v0.3 | 2026-05-30 | 股票四視圖切換、Finnhub 股價回寫快照、GitHub Pages 密碼保護、GitHub icon |
